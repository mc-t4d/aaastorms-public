# report.yml

report:
  description: "This function reads storm data and sends a report"
  timeout: 300
  memorySize: 725
  handler: report_main.handler
  runtime: python3.9
  layers:
    - !Ref DependenciesLambdaLayer
